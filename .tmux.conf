set -g mouse on                 # enable mouse scrolling (does not affect vim!)
set -g history-limit 30000      # set scrollback buffer size

bind | split-window -h
bind - split-window -v

# Start windows and panes at 1, not 0
#set -g base-index 1
#setw -g pane-base-index 1

